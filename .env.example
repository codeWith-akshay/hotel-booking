# ==========================================
# HOTEL BOOKING - ENVIRONMENT VARIABLES
# ==========================================

# ==========================================
# DATABASE
# ==========================================

# SQLite database file location
DATABASE_URL="file:./dev.db"

# ==========================================
# STRIPE PAYMENT INTEGRATION
# ==========================================

# Stripe Secret Key (server-side only)
# Get from: https://dashboard.stripe.com/test/apikeys
# For production: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_REPLACE_WITH_YOUR_ACTUAL_STRIPE_SECRET_KEY

# Stripe Webhook Secret (for signature verification)
# Local development with Stripe CLI:
#   1. Install: https://stripe.com/docs/stripe-cli
#   2. Login: stripe login
#   3. Forward webhooks: stripe listen --forward-to localhost:3000/api/webhooks/stripe
#   4. Copy webhook signing secret (starts with whsec_)
# Production: Create webhook at https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_REPLACE_WITH_YOUR_WEBHOOK_SECRET

# Stripe Publishable Key (client-side, safe to expose)
# Get from: https://dashboard.stripe.com/test/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_REPLACE_WITH_YOUR_PUBLISHABLE_KEY

# Base URL for payment redirects
# Local: http://localhost:3000
# Development: Use ngrok or similar for webhook testing
# Production: https://your-domain.com
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ==========================================
# AUTHENTICATION (if applicable)
# ==========================================

# NextAuth secret (generate with: openssl rand -base64 32)
# NEXTAUTH_SECRET=your-secret-here
# NEXTAUTH_URL=http://localhost:3000

# ==========================================
# EMAIL (for booking confirmations)
# ==========================================

# Email service configuration (e.g., SendGrid, Resend, etc.)
# EMAIL_FROM=noreply@example.com
# EMAIL_API_KEY=your-email-api-key

# ==========================================
# NOTIFICATION SYSTEM
# ==========================================

# NodeMailer SMTP Configuration (for email notifications)
# Gmail example (requires App Password if using Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM="Hotel Booking <your-email@gmail.com>"

# Twilio WhatsApp Configuration (for WhatsApp notifications)
# Get from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your-twilio-account-sid
TWILIO_AUTH_TOKEN=your-twilio-auth-token
TWILIO_WHATSAPP_NUMBER=+14155238886

# ==========================================
# IRCA MEMBERSHIP API
# ==========================================

# IRCA API Base URL (publicly accessible)
# Contact IRCA support for production endpoint
# Example: https://api.irca.org or https://membership.irca.org/api
NEXT_PUBLIC_IRCA_API_URL=https://api.irca.org

# IRCA API Authentication Key (keep secret, server-side only)
# Obtain from IRCA admin panel or contact IRCA support
IRCA_API_KEY=your-irca-api-key-here

# IRCA API Timeout (milliseconds) - Optional
# Default: 10000 (10 seconds)
IRCA_API_TIMEOUT=10000

# IRCA API Retry Attempts - Optional
# Number of retry attempts for failed requests
# Default: 3
IRCA_API_RETRY_ATTEMPTS=3

# IRCA API Retry Delay (milliseconds) - Optional
# Delay between retries (uses exponential backoff)
# Default: 1000 (1 second)
IRCA_API_RETRY_DELAY=1000

# IRCA API Cache Enabled - Optional
# Enable caching to reduce API calls
# Default: true
IRCA_API_CACHE_ENABLED=true

# IRCA API Cache Duration (milliseconds) - Optional
# How long to cache membership data
# Default: 300000 (5 minutes)
IRCA_API_CACHE_DURATION=300000

# ==========================================
# OTHER
# ==========================================

# Node environment
NODE_ENV=development

# ==========================================
# QUICK START - STRIPE TEST MODE
# ==========================================
# 
# 1. Sign up for Stripe: https://dashboard.stripe.com/register
# 2. Enable "Test mode" (toggle in top-right)
# 3. Get API keys: https://dashboard.stripe.com/test/apikeys
# 4. Copy keys to this file
# 5. Install Stripe CLI: https://stripe.com/docs/stripe-cli
# 6. Run: stripe listen --forward-to localhost:3000/api/webhooks/stripe
# 7. Copy webhook secret to STRIPE_WEBHOOK_SECRET
# 8. Test with card: 4242 4242 4242 4242
# 
# Test Cards:
# - Success: 4242 4242 4242 4242
# - Decline: 4000 0000 0000 0002
# - 3D Secure: 4000 0025 0000 3155
# 
# Use any future date (12/34), any CVC (123), any ZIP (12345)
# 
# More: https://stripe.com/docs/testing
